[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "oworker-aclip"
version = "0.3.5"
description = "Python reference SDK for the Agent Command Line Interface Protocol"
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
authors = [
  { name = "OWorker" }
]
keywords = ["aclip", "cli", "agents", "sdk"]
classifiers = [
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.12",
  "Topic :: Software Development :: Libraries",
]
dependencies = [
  "click>=8.1.8",
]

[project.urls]
Homepage = "https://github.com/oworker-dev/oworker-aclip"
Repository = "https://github.com/oworker-dev/oworker-aclip"
Issues = "https://github.com/oworker-dev/oworker-aclip/issues"

[project.optional-dependencies]
dev = [
  "build>=1.2.2",
  "jsonschema>=4.25.1",
  "pytest>=8.3.5",
  "pyinstaller>=6.14.2",
  "twine>=6.1.0",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.package-data]
aclip = ["_schema/*.json"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]


